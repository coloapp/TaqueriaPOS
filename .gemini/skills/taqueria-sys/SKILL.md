# Skill: Taqueria Dependency & System Expert (node_modules/ & configs)
Expertise in project dependencies, Capacitor configurations, and system-level integrations (Bluetooth, HTTP server).

## Efficiency Guidelines
- **No-Read Policy:** NEVER read the content of `node_modules`. Rely on `package.json` for versioning and official documentation for API usage.
- **Config Management:** Rigorously protect `capacitor.config.json` and system files.
- **Build Flow:** Use `compilar.bat` as the reference for the build and sync process between Web and Native.

## Sub-agent Instructions
Invoke a sub-agent with this skill context for fixing dependency issues or investigating Capacitor plugin failures.
